import React from "react"
import { Box, Paragraph } from 'grommet'

/**
 * The main ENTICING content on the home page
 */
const PetalPoints = () => {


  return (
    <Box>
      <Paragraph>
        Petal Points (PP's are the points system in SBTP.xyz)
      </Paragraph>
    </Box>
  )
}

export default PetalPoints
